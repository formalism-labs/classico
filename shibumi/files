
if [[ $(platform_os) == macos ]]; then

realpath() {
	[[ $1 = /* ]] && echo "$1" || echo "$PWD/${1#./}"
}

fi

#----------------------------------------------------------------------------------------------

is_abspath() {
	case $1 in (/*) pathchk -- "$1";; (*) ! : ;; esac
}

#----------------------------------------------------------------------------------------------
